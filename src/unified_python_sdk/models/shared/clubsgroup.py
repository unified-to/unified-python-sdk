"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .property_clubsgroup_address import (
    PropertyClubsGroupAddress,
    PropertyClubsGroupAddressTypedDict,
)
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class GroupType(str, Enum, metaclass=utils.OpenEnumMeta):
    CASUAL_CLUB = "CASUAL_CLUB"
    RACING_TEAM = "RACING_TEAM"
    SHOP = "SHOP"
    COMPANY = "COMPANY"
    OTHER = "OTHER"


class MembershipStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    MEMBER = "MEMBER"
    PENDING = "PENDING"
    NONE = "NONE"


class SportType(str, Enum, metaclass=utils.OpenEnumMeta):
    RUNNING = "RUNNING"
    CYCLING = "CYCLING"
    SWIMMING = "SWIMMING"
    TRIATHLON = "TRIATHLON"
    WALKING = "WALKING"
    HIKING = "HIKING"
    OTHER = "OTHER"


class ClubsGroupTypedDict(TypedDict):
    r"""SummaryClub / DetailedClub, but generic across providers (TeamSnap team, PlayHQ org, etc.)."""

    activity_types: NotRequired[List[str]]
    r"""The activity types the group focuses on"""
    address: NotRequired[PropertyClubsGroupAddressTypedDict]
    r"""The group's address"""
    cover_image_url: NotRequired[str]
    created_at: NotRequired[datetime]
    description: NotRequired[str]
    following_count: NotRequired[float]
    group_type: NotRequired[GroupType]
    id: NotRequired[str]
    is_admin: NotRequired[bool]
    is_featured: NotRequired[bool]
    is_owner: NotRequired[bool]
    is_private: NotRequired[bool]
    is_verified: NotRequired[bool]
    member_count: NotRequired[float]
    membership_status: NotRequired[MembershipStatus]
    name: NotRequired[str]
    post_count: NotRequired[float]
    profile_image_url: NotRequired[str]
    raw: NotRequired[Dict[str, Any]]
    sport_type: NotRequired[SportType]
    updated_at: NotRequired[datetime]
    url: NotRequired[str]


class ClubsGroup(BaseModel):
    r"""SummaryClub / DetailedClub, but generic across providers (TeamSnap team, PlayHQ org, etc.)."""

    activity_types: Optional[List[str]] = None
    r"""The activity types the group focuses on"""

    address: Optional[PropertyClubsGroupAddress] = None
    r"""The group's address"""

    cover_image_url: Optional[str] = None

    created_at: Optional[datetime] = None

    description: Optional[str] = None

    following_count: Optional[float] = None

    group_type: Optional[GroupType] = None

    id: Optional[str] = None

    is_admin: Optional[bool] = None

    is_featured: Optional[bool] = None

    is_owner: Optional[bool] = None

    is_private: Optional[bool] = None

    is_verified: Optional[bool] = None

    member_count: Optional[float] = None

    membership_status: Optional[MembershipStatus] = None

    name: Optional[str] = None

    post_count: Optional[float] = None

    profile_image_url: Optional[str] = None

    raw: Optional[Dict[str, Any]] = None

    sport_type: Optional[SportType] = None

    updated_at: Optional[datetime] = None

    url: Optional[str] = None

    @field_serializer("group_type")
    def serialize_group_type(self, value):
        if isinstance(value, str):
            try:
                return shared.GroupType(value)
            except ValueError:
                return value
        return value

    @field_serializer("membership_status")
    def serialize_membership_status(self, value):
        if isinstance(value, str):
            try:
                return shared.MembershipStatus(value)
            except ValueError:
                return value
        return value

    @field_serializer("sport_type")
    def serialize_sport_type(self, value):
        if isinstance(value, str):
            try:
                return shared.SportType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "activity_types",
                "address",
                "cover_image_url",
                "created_at",
                "description",
                "following_count",
                "group_type",
                "id",
                "is_admin",
                "is_featured",
                "is_owner",
                "is_private",
                "is_verified",
                "member_count",
                "membership_status",
                "name",
                "post_count",
                "profile_image_url",
                "raw",
                "sport_type",
                "updated_at",
                "url",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
