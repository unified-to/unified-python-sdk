"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class AdsPromotedType(str, Enum, metaclass=utils.OpenEnumMeta):
    PAGE_ID = "PAGE_ID"
    APP_ID = "APP_ID"
    STORE_URL = "STORE_URL"
    PIXEL_ID = "PIXEL_ID"
    CUSTOM_CONVERSION_ID = "CUSTOM_CONVERSION_ID"
    CATALOG_ID = "CATALOG_ID"
    PRODUCT_SET_ID = "PRODUCT_SET_ID"
    PRIORITIZED_SET_ID = "PRIORITIZED_SET_ID"
    EVENT_ID = "EVENT_ID"
    OFFER_ID = "OFFER_ID"
    LEAD_FORM_ID = "LEAD_FORM_ID"
    MESSAGING_CHANNEL_ID = "MESSAGING_CHANNEL_ID"
    PRODUCT_ID = "PRODUCT_ID"
    TWEET_ID = "TWEET_ID"
    AD_GROUP_TYPE = "AD_GROUP_TYPE"


class AdsPromotedTypedDict(TypedDict):
    r"""Promoted entity (e.g. page, app, product, tweet) for ads create"""

    id: str
    type: AdsPromotedType
    name: NotRequired[str]


class AdsPromoted(BaseModel):
    r"""Promoted entity (e.g. page, app, product, tweet) for ads create"""

    id: str

    type: AdsPromotedType

    name: Optional[str] = None

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return shared.AdsPromotedType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
