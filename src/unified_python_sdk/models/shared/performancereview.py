"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .performancereviewquestion import (
    PerformanceReviewQuestion,
    PerformanceReviewQuestionTypedDict,
)
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class PerformanceReviewStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    DRAFT = "DRAFT"
    IN_PROGRESS = "IN_PROGRESS"
    SUBMITTED = "SUBMITTED"
    COMPLETED = "COMPLETED"
    SHARED = "SHARED"
    ACKNOWLEDGED = "ACKNOWLEDGED"
    OTHER = "OTHER"


class PerformanceReviewType(str, Enum, metaclass=utils.OpenEnumMeta):
    SELF = "SELF"
    MANAGER = "MANAGER"
    PEER = "PEER"
    UPWARD = "UPWARD"
    OTHER = "OTHER"


class PerformanceReviewTypedDict(TypedDict):
    user_id: str
    comment: NotRequired[str]
    completed_at: NotRequired[datetime]
    created_at: NotRequired[datetime]
    cycle_id: NotRequired[str]
    id: NotRequired[str]
    is_acknowledged: NotRequired[bool]
    questions: NotRequired[List[PerformanceReviewQuestionTypedDict]]
    rating: NotRequired[float]
    rating_description: NotRequired[str]
    rating_maximum: NotRequired[float]
    raw: NotRequired[Dict[str, Any]]
    reviewer_user_id: NotRequired[str]
    status: NotRequired[PerformanceReviewStatus]
    submitted_at: NotRequired[datetime]
    template_identifier: NotRequired[str]
    type: NotRequired[PerformanceReviewType]
    updated_at: NotRequired[datetime]


class PerformanceReview(BaseModel):
    user_id: str

    comment: Optional[str] = None

    completed_at: Optional[datetime] = None

    created_at: Optional[datetime] = None

    cycle_id: Optional[str] = None

    id: Optional[str] = None

    is_acknowledged: Optional[bool] = None

    questions: Optional[List[PerformanceReviewQuestion]] = None

    rating: Optional[float] = None

    rating_description: Optional[str] = None

    rating_maximum: Optional[float] = None

    raw: Optional[Dict[str, Any]] = None

    reviewer_user_id: Optional[str] = None

    status: Optional[PerformanceReviewStatus] = None

    submitted_at: Optional[datetime] = None

    template_identifier: Optional[str] = None

    type: Optional[PerformanceReviewType] = None

    updated_at: Optional[datetime] = None

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return shared.PerformanceReviewStatus(value)
            except ValueError:
                return value
        return value

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return shared.PerformanceReviewType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "comment",
                "completed_at",
                "created_at",
                "cycle_id",
                "id",
                "is_acknowledged",
                "questions",
                "rating",
                "rating_description",
                "rating_maximum",
                "raw",
                "reviewer_user_id",
                "status",
                "submitted_at",
                "template_identifier",
                "type",
                "updated_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
