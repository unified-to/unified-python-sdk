"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class CalendarWebinarPanelistStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    ACCEPTED = "ACCEPTED"
    REJECTED = "REJECTED"
    TENTATIVE = "TENTATIVE"


class CalendarWebinarPanelistTypedDict(TypedDict):
    email: NotRequired[str]
    is_required: NotRequired[bool]
    join_password: NotRequired[str]
    join_url: NotRequired[str]
    name: NotRequired[str]
    status: NotRequired[CalendarWebinarPanelistStatus]


class CalendarWebinarPanelist(BaseModel):
    email: Optional[str] = None

    is_required: Optional[bool] = None

    join_password: Optional[str] = None

    join_url: Optional[str] = None

    name: Optional[str] = None

    status: Optional[CalendarWebinarPanelistStatus] = None

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return shared.CalendarWebinarPanelistStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["email", "is_required", "join_password", "join_url", "name", "status"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
