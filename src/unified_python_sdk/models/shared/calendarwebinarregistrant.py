"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class RegistrationStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    PENDING = "PENDING"
    APPROVED = "APPROVED"
    REJECTED = "REJECTED"
    CANCELLED = "CANCELLED"


class CalendarWebinarRegistrantTypedDict(TypedDict):
    email: NotRequired[str]
    name: NotRequired[str]
    registered_at: NotRequired[datetime]
    registration_reference: NotRequired[str]
    registration_status: NotRequired[RegistrationStatus]


class CalendarWebinarRegistrant(BaseModel):
    email: Optional[str] = None

    name: Optional[str] = None

    registered_at: Optional[datetime] = None

    registration_reference: Optional[str] = None

    registration_status: Optional[RegistrationStatus] = None

    @field_serializer("registration_status")
    def serialize_registration_status(self, value):
        if isinstance(value, str):
            try:
                return shared.RegistrationStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "email",
                "name",
                "registered_at",
                "registration_reference",
                "registration_status",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
