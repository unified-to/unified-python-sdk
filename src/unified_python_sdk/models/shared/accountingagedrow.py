"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .property_accountingagedrow_contact import (
    PropertyAccountingAgedRowContact,
    PropertyAccountingAgedRowContactTypedDict,
)
from .property_accountingagedrow_transaction import (
    PropertyAccountingAgedRowTransaction,
    PropertyAccountingAgedRowTransactionTypedDict,
)
from datetime import datetime
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class AccountingAgedRowTypedDict(TypedDict):
    balance_amount: NotRequired[float]
    contact: NotRequired[PropertyAccountingAgedRowContactTypedDict]
    due_at: NotRequired[datetime]
    posted_at: NotRequired[datetime]
    total_amount: NotRequired[float]
    transaction: NotRequired[PropertyAccountingAgedRowTransactionTypedDict]


class AccountingAgedRow(BaseModel):
    balance_amount: Optional[float] = None

    contact: Optional[PropertyAccountingAgedRowContact] = None

    due_at: Optional[datetime] = None

    posted_at: Optional[datetime] = None

    total_amount: Optional[float] = None

    transaction: Optional[PropertyAccountingAgedRowTransaction] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "balance_amount",
                "contact",
                "due_at",
                "posted_at",
                "total_amount",
                "transaction",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
