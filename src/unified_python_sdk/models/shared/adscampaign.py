"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .property_adscampaign_frequency_cap import (
    PropertyAdsCampaignFrequencyCap,
    PropertyAdsCampaignFrequencyCapTypedDict,
)
from .property_adscampaign_targeting import (
    PropertyAdsCampaignTargeting,
    PropertyAdsCampaignTargetingTypedDict,
)
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class BudgetPeriod(str, Enum, metaclass=utils.OpenEnumMeta):
    DAILY = "DAILY"
    MONTHLY = "MONTHLY"
    TOTAL = "TOTAL"
    LIFETIME = "LIFETIME"


class Goal(str, Enum, metaclass=utils.OpenEnumMeta):
    UNSPECIFIED = "UNSPECIFIED"
    BRAND_AWARENESS = "BRAND_AWARENESS"
    REACH = "REACH"
    WEBSITE_TRAFFIC = "WEBSITE_TRAFFIC"
    LEADS = "LEADS"
    SALES = "SALES"
    APP_PROMOTION = "APP_PROMOTION"


class AdsCampaignStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    UNSPECIFIED = "UNSPECIFIED"
    ACTIVE = "ACTIVE"
    PAUSED = "PAUSED"
    ARCHIVED = "ARCHIVED"
    DRAFT = "DRAFT"
    SCHEDULED_FOR_DELETION = "SCHEDULED_FOR_DELETION"


class AdsCampaignTypedDict(TypedDict):
    budget_amount: NotRequired[float]
    budget_period: NotRequired[BudgetPeriod]
    created_at: NotRequired[datetime]
    end_at: NotRequired[datetime]
    frequency_cap: NotRequired[PropertyAdsCampaignFrequencyCapTypedDict]
    goal: NotRequired[Goal]
    id: NotRequired[str]
    is_active: NotRequired[bool]
    name: NotRequired[str]
    organization_id: NotRequired[str]
    planned_spend_amount: NotRequired[float]
    raw: NotRequired[Dict[str, Any]]
    start_at: NotRequired[datetime]
    status: NotRequired[AdsCampaignStatus]
    targeting: NotRequired[PropertyAdsCampaignTargetingTypedDict]
    total_spend_amount: NotRequired[float]
    updated_at: NotRequired[datetime]


class AdsCampaign(BaseModel):
    budget_amount: Optional[float] = None

    budget_period: Optional[BudgetPeriod] = None

    created_at: Optional[datetime] = None

    end_at: Optional[datetime] = None

    frequency_cap: Optional[PropertyAdsCampaignFrequencyCap] = None

    goal: Optional[Goal] = None

    id: Optional[str] = None

    is_active: Optional[bool] = None

    name: Optional[str] = None

    organization_id: Optional[str] = None

    planned_spend_amount: Optional[float] = None

    raw: Optional[Dict[str, Any]] = None

    start_at: Optional[datetime] = None

    status: Optional[AdsCampaignStatus] = None

    targeting: Optional[PropertyAdsCampaignTargeting] = None

    total_spend_amount: Optional[float] = None

    updated_at: Optional[datetime] = None

    @field_serializer("budget_period")
    def serialize_budget_period(self, value):
        if isinstance(value, str):
            try:
                return shared.BudgetPeriod(value)
            except ValueError:
                return value
        return value

    @field_serializer("goal")
    def serialize_goal(self, value):
        if isinstance(value, str):
            try:
                return shared.Goal(value)
            except ValueError:
                return value
        return value

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return shared.AdsCampaignStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "budget_amount",
                "budget_period",
                "created_at",
                "end_at",
                "frequency_cap",
                "goal",
                "id",
                "is_active",
                "name",
                "organization_id",
                "planned_spend_amount",
                "raw",
                "start_at",
                "status",
                "targeting",
                "total_spend_amount",
                "updated_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
