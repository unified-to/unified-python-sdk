"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class PerformanceReviewQuestionType(str, Enum, metaclass=utils.OpenEnumMeta):
    TEXT = "TEXT"
    SINGLE_SELECT = "SINGLE_SELECT"
    MULTI_SELECT = "MULTI_SELECT"
    RATING = "RATING"
    OTHER = "OTHER"


class PerformanceReviewQuestionTypedDict(TypedDict):
    question: str
    answers: NotRequired[List[str]]
    r"""all answer values; single-answer questions have one element"""
    description: NotRequired[str]
    type: NotRequired[PerformanceReviewQuestionType]


class PerformanceReviewQuestion(BaseModel):
    question: str

    answers: Optional[List[str]] = None
    r"""all answer values; single-answer questions have one element"""

    description: Optional[str] = None

    type: Optional[PerformanceReviewQuestionType] = None

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return shared.PerformanceReviewQuestionType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["answers", "description", "type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
