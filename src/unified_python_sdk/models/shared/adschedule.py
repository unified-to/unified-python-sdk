"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class DayOfWeek(str, Enum, metaclass=utils.OpenEnumMeta):
    MONDAY = "MONDAY"
    TUESDAY = "TUESDAY"
    WEDNESDAY = "WEDNESDAY"
    THURSDAY = "THURSDAY"
    FRIDAY = "FRIDAY"
    SATURDAY = "SATURDAY"
    SUNDAY = "SUNDAY"


class AdScheduleTypedDict(TypedDict):
    day_of_week: DayOfWeek
    end_hour: float
    start_hour: float
    bid_modifier: NotRequired[float]
    end_minute: NotRequired[float]
    start_minute: NotRequired[float]


class AdSchedule(BaseModel):
    day_of_week: DayOfWeek

    end_hour: float

    start_hour: float

    bid_modifier: Optional[float] = None

    end_minute: Optional[float] = None

    start_minute: Optional[float] = None

    @field_serializer("day_of_week")
    def serialize_day_of_week(self, value):
        if isinstance(value, str):
            try:
                return shared.DayOfWeek(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["bid_modifier", "end_minute", "start_minute"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
