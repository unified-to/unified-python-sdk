"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class AccountingFeeType(str, Enum, metaclass=utils.OpenEnumMeta):
    TAX = "TAX"
    DISCOUNT = "DISCOUNT"
    PROMOTION = "PROMOTION"
    SHIPPING = "SHIPPING"
    GIFT_WRAP = "GIFT_WRAP"
    COD = "COD"
    SURCHARGE = "SURCHARGE"
    OTHER = "OTHER"


class AccountingFeeTypedDict(TypedDict):
    amount: float
    type: AccountingFeeType
    currency: NotRequired[str]
    original_type: NotRequired[str]


class AccountingFee(BaseModel):
    amount: float

    type: AccountingFeeType

    currency: Optional[str] = None

    original_type: Optional[str] = None

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return shared.AccountingFeeType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["currency", "original_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
