"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .accountingmetadata import AccountingMetadata, AccountingMetadataTypedDict
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class BillingType(str, Enum, metaclass=utils.OpenEnumMeta):
    FIXED_PRICE = "FIXED_PRICE"
    TIME_AND_MATERIALS = "TIME_AND_MATERIALS"
    MILESTONE = "MILESTONE"
    NON_BILLABLE = "NON_BILLABLE"


class AccountingProjectStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    NOT_STARTED = "NOT_STARTED"
    IN_PROGRESS = "IN_PROGRESS"
    ON_HOLD = "ON_HOLD"
    COMPLETED = "COMPLETED"
    CANCELLED = "CANCELLED"
    CLOSED = "CLOSED"


class AccountingProjectTypedDict(TypedDict):
    actual_cost_amount: NotRequired[float]
    billing_type: NotRequired[BillingType]
    category_ids: NotRequired[List[str]]
    code: NotRequired[str]
    contact_id: NotRequired[str]
    created_at: NotRequired[datetime]
    credit_note_amount: NotRequired[float]
    currency: NotRequired[str]
    deposit_amount: NotRequired[float]
    deposit_applied_amount: NotRequired[float]
    description: NotRequired[str]
    due_at: NotRequired[datetime]
    ended_at: NotRequired[datetime]
    estimate_amount: NotRequired[float]
    estimated_cost_amount: NotRequired[float]
    expense_amount: NotRequired[float]
    id: NotRequired[str]
    invoiced_amount: NotRequired[float]
    is_active: NotRequired[bool]
    is_billable: NotRequired[bool]
    location_id: NotRequired[str]
    manager_user_id: NotRequired[str]
    metadata: NotRequired[List[AccountingMetadataTypedDict]]
    minutes_logged: NotRequired[float]
    minutes_to_be_invoiced: NotRequired[float]
    name: NotRequired[str]
    organization_id: NotRequired[str]
    parent_id: NotRequired[str]
    percent_complete: NotRequired[float]
    raw: NotRequired[Dict[str, Any]]
    started_at: NotRequired[datetime]
    status: NotRequired[AccountingProjectStatus]
    to_be_invoiced_amount: NotRequired[float]
    updated_at: NotRequired[datetime]


class AccountingProject(BaseModel):
    actual_cost_amount: Optional[float] = None

    billing_type: Optional[BillingType] = None

    category_ids: Optional[List[str]] = None

    code: Optional[str] = None

    contact_id: Optional[str] = None

    created_at: Optional[datetime] = None

    credit_note_amount: Optional[float] = None

    currency: Optional[str] = None

    deposit_amount: Optional[float] = None

    deposit_applied_amount: Optional[float] = None

    description: Optional[str] = None

    due_at: Optional[datetime] = None

    ended_at: Optional[datetime] = None

    estimate_amount: Optional[float] = None

    estimated_cost_amount: Optional[float] = None

    expense_amount: Optional[float] = None

    id: Optional[str] = None

    invoiced_amount: Optional[float] = None

    is_active: Optional[bool] = None

    is_billable: Optional[bool] = None

    location_id: Optional[str] = None

    manager_user_id: Optional[str] = None

    metadata: Optional[List[AccountingMetadata]] = None

    minutes_logged: Optional[float] = None

    minutes_to_be_invoiced: Optional[float] = None

    name: Optional[str] = None

    organization_id: Optional[str] = None

    parent_id: Optional[str] = None

    percent_complete: Optional[float] = None

    raw: Optional[Dict[str, Any]] = None

    started_at: Optional[datetime] = None

    status: Optional[AccountingProjectStatus] = None

    to_be_invoiced_amount: Optional[float] = None

    updated_at: Optional[datetime] = None

    @field_serializer("billing_type")
    def serialize_billing_type(self, value):
        if isinstance(value, str):
            try:
                return shared.BillingType(value)
            except ValueError:
                return value
        return value

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return shared.AccountingProjectStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "actual_cost_amount",
                "billing_type",
                "category_ids",
                "code",
                "contact_id",
                "created_at",
                "credit_note_amount",
                "currency",
                "deposit_amount",
                "deposit_applied_amount",
                "description",
                "due_at",
                "ended_at",
                "estimate_amount",
                "estimated_cost_amount",
                "expense_amount",
                "id",
                "invoiced_amount",
                "is_active",
                "is_billable",
                "location_id",
                "manager_user_id",
                "metadata",
                "minutes_logged",
                "minutes_to_be_invoiced",
                "name",
                "organization_id",
                "parent_id",
                "percent_complete",
                "raw",
                "started_at",
                "status",
                "to_be_invoiced_amount",
                "updated_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
