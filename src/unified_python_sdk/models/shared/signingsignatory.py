"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, Optional
from typing_extensions import NotRequired, TypedDict
from unified_python_sdk import utils
from unified_python_sdk.models import shared
from unified_python_sdk.types import BaseModel, UNSET_SENTINEL


class SigningSignatoryRole(str, Enum, metaclass=utils.OpenEnumMeta):
    SIGNER = "SIGNER"
    CC = "CC"
    APPROVER = "APPROVER"
    IN_PERSON_SIGNER = "IN_PERSON_SIGNER"
    VIEWER = "VIEWER"


class SigningSignatoryStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    PENDING = "PENDING"
    SENT = "SENT"
    DELIVERED = "DELIVERED"
    SIGNED = "SIGNED"
    DECLINED = "DECLINED"
    ERROR = "ERROR"


class SigningSignatoryTypedDict(TypedDict):
    created_at: NotRequired[datetime]
    decline_reason: NotRequired[str]
    document_id: NotRequired[str]
    email: NotRequired[str]
    id: NotRequired[str]
    name: NotRequired[str]
    order: NotRequired[float]
    raw: NotRequired[Dict[str, Any]]
    role: NotRequired[SigningSignatoryRole]
    signed_at: NotRequired[datetime]
    status: NotRequired[SigningSignatoryStatus]
    updated_at: NotRequired[datetime]


class SigningSignatory(BaseModel):
    created_at: Optional[datetime] = None

    decline_reason: Optional[str] = None

    document_id: Optional[str] = None

    email: Optional[str] = None

    id: Optional[str] = None

    name: Optional[str] = None

    order: Optional[float] = None

    raw: Optional[Dict[str, Any]] = None

    role: Optional[SigningSignatoryRole] = None

    signed_at: Optional[datetime] = None

    status: Optional[SigningSignatoryStatus] = None

    updated_at: Optional[datetime] = None

    @field_serializer("role")
    def serialize_role(self, value):
        if isinstance(value, str):
            try:
                return shared.SigningSignatoryRole(value)
            except ValueError:
                return value
        return value

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return shared.SigningSignatoryStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "created_at",
                "decline_reason",
                "document_id",
                "email",
                "id",
                "name",
                "order",
                "raw",
                "role",
                "signed_at",
                "status",
                "updated_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
